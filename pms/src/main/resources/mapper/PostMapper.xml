<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dev.pms.mapper.PostMapper">
    <select id="getPostsByTitle" resultType="PostVo">
        select *
        from post_table
        <where>
            <if test="title != null and title != ''">
                and title like concat('%',#{title},'%')
            </if>
        </where>
    </select>

    <select id="getPostsByWriter" resultType="PostVo">
        select *
        from post_table
        <where>
            <if test="writer != null and writer != ''">
                and writer like concat('%',#{writer},'%')
            </if>
        </where>
    </select>

    <select id="getPostsByCond" resultType="PostVo">
        select *
        from post_table
        <where>
            <if test="title != null and title != ''">
                and title like concat('%',#{title},'%')
            </if>
            <if test="writer != null and writer != ''">
                and writer like concat('%',#{writer},'%')
            </if>
        </where>
    </select>

    <select id="getPostById" resultType="PostVo">
        select *
        from post_table
        where post_id=#{postId}
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="postId">
        insert into post_table
        (title, writer, post_body, post_date)
        values
        (#{title}, #{writer}, #{post_body}, current_timestamp)
    </insert>

    <update id="update">
        update post_table
        set title=#{updateParam.title},
            post_body=#{updateParam.postBody},
            post_date=current_timestamp
        where post_id=#{postId}
    </update>

    <delete id="delete">
        delete
        from post_table
        where post_id=#{postId}
    </delete>

    <select id="getWriter" resultType="String">
        select name
        from user_table
        where user_id=#{writerId}
    </select>
</mapper>